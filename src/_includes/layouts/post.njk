---
layout: layouts/base.njk
---
<div class="entry-body">
  <header class="post-header">
    <h1 class="main-title">{{ title | safe }}</h1>
    <p class="meta-text"><time datetime="{{ post.date | htmlDateString }}">{{ page.date | readableDate }}</time> <span class="reading-time">{{ helpers.getReadingTime(content) }}m</span></p>
  </header>
</div>
{{ content | safe }}

